apiVersion: v1
kind: Service
metadata:
  name: {{ include "apppython-minikube.fullname" . }}
  labels:
    {{- include "apppython-minikube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apppython-minikube.selectorLabels" . | nindent 4 }}
