# Deployment
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: apppython
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: apppython
  template:
    metadata:
      labels:
        app: apppython
    spec:
      securityContext:
        runAsUser: 1001
        runAsGroup: 3000
        fsGroup: 2000    
      restartPolicy: Always
      containers:
      - name: apppython
        image:  garrijuan/test_api_python:latest 
        #env:
        #- name: TOPO_PORT
         # value: "8181"
        ports:
        - containerPort: 80
  replicas: 1

---
# Service
apiVersion: v1
kind: Service
metadata:
  name: apppython
  labels:
    app: apppython
spec:
  selector:
    app: apppython
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: apppython-port
  type: ClusterIP